export type ProjectProps = {
    title: string,
    img?: string,
    href?: string,
    type?: string,
    releaseDate?: number,
    isOngoing?: boolean,
    id?: any,
    description?: string,
    links?: any[]
}