export type ProjectProps = {
    title: string,
    img?: string,
    href?: string,
    type?: string,
    releaseDate?: number,
    id?: any,
    description?: string,
    links?: any[]
    status: string,
}